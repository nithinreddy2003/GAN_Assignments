# 7COM1079 Coursework 2 (Set 3) — dependencies
# Install with:  pip install -r requirements.txt
# The notebook installs these itself in its first cell, so this file is mainly
# for setting up a clean environment ahead of time.
#
# Framework split: Part 1 and Part 2.3 run on PyTorch; Part 2.1 and Part 2.2
# run on TensorFlow/Keras. A CUDA GPU is recommended for the Part 2 models.

# --- Deep-learning frameworks ---
torch
torchvision
tensorflow[and-cuda]      # GPU build; on a CPU-only machine use plain `tensorflow`

# --- Numerical / data handling ---
numpy
pandas
scipy                     # scipy.linalg.sqrtm is used in the FID computation
scikit-learn              # PCA, t-SNE and StandardScaler for Part 2.2

# --- Datasets ---
medmnist                  # OCTMNIST for Part 2.1 (auto-downloads on first run)
kagglehub                 # CICIDS 2017 for Part 2.2 (needs Kaggle credentials — see README)

# --- Plotting and utilities ---
matplotlib
pillow
tqdm
requests
