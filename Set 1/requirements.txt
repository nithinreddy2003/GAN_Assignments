# ── Set 1 dependencies (grouped by role) ───────────────────────────────
# tensorflow[and-cuda] targets an NVIDIA GPU; swap for plain `tensorflow` on CPU.

# numerical / data handling
numpy
pandas
scipy
scikit-learn

# plotting
matplotlib

# deep-learning frameworks
torch
torchvision
tensorflow[and-cuda]

# datasets
medmnist                 # OCTMNIST — auto-downloads on first run
kagglehub                # CICIDS 2017 — needs a Kaggle API token (see README)

# download / progress-bar utilities
requests
tqdm
pillow
