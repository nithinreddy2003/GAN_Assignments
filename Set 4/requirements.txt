# Set 4 requirements — organised by which stack pulls them in.
# Install order matters a little: install the Torch stack first, then TensorFlow,
# so the CUDA wheels resolve cleanly. On CPU-only machines drop the "[and-cuda]".

# PyTorch stack (Part 1, Part 2.1, Part 2.3 — incl. InceptionV3 FID)
torch
torchvision

# TensorFlow stack (Part 2.2 tabular GAN)
tensorflow[and-cuda]

# dataset access
medmnist        # OCTMNIST
kagglehub       # CICIDS 2017

# shared scientific-Python stack
numpy
pandas
scipy
scikit-learn
matplotlib
pillow
requests
tqdm
